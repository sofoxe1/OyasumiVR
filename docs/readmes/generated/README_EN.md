linux port of https://github.com/Raphiiko/OyasumiVR targeting monado

### Buidling from source:
* clone the repo
* `npm run init`
* `npm run release` or `NO_PACKAGE=1 npm run release` if you don't want tar archive



some of the dependencies are fetched over ssh and require an account on codeberg.org
